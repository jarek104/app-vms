export interface Ivm {
  vmID: number;
  vmName: string;
  vmCurrentUser: string;
  vmIP: string;
  vmOS: string;
  vmDotNet: string;
  vmBuildInstalled: string;
  vmComment: string;
  vmLauncher: string;
}
